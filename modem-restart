#!/bin/bash
# Modem Restart Script
# Restarts the modem with a single command
#
# Usage: modem-restart
# Or copy this file to /usr/local/bin/modem-restart

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

node restart.js "$@"
